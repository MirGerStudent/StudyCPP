#include "RingBufferList.h"

template <typename T> 
RingBufferList<T>::RingBufferList() : Buff{nullptr}

template <typename T> 
void RingBufferList<T>::push(const T &value, ListIterator<T> &it) {
    
}